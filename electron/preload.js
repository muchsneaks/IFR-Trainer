'use strict';

// The map UI is a plain web page served over localhost and needs no Node
// APIs, so the preload deliberately exposes nothing. It exists only to keep
// contextIsolation on with an explicit, empty bridge.
